# 10. 堆分配

> **原文**：https://os.phil-opp.com/heap-allocation/  
> **参考代码分支**：post-10  
> **难度**：⭐⭐⭐⭐⭐

---

## 📚 教程目标

本教程为我们的内核添加堆分配支持。首先，它介绍动态内存，并展示借用检查器如何防止常见的分配错误。然后实现 Rust 的基本分配接口，创建堆内存区域，并设置分配器 crate。在本教程结束时，内置 `alloc` crate 的所有分配和集合类型都将可用于我们的内核。

在本教程结束时，您将：
- 理解动态内存分配
- 实现全局分配器
- 创建堆内存区域
- 使用 Vec、String 等集合类型

---

## 🎯 概述

堆分配允许动态分配内存，这对于实现以下功能至关重要：
- **动态数据结构**：Vec、String、HashMap 等
- **动态大小对象**：运行时才知道大小的对象
- **灵活的内存管理**：按需分配和释放

---

## 📝 实现步骤

### 步骤 1：创建堆分配器

创建 `src/allocator.rs`：

```rust
use linked_list_allocator::LockedHeap;

#[global_allocator]
static ALLOCATOR: LockedHeap = LockedHeap::empty();

pub const HEAP_START: usize = 0x_4444_4444_0000;
pub const HEAP_SIZE: usize = 100 * 1024; // 100 KiB

pub fn init_heap(
    mapper: &mut impl Mapper<Size4KiB>,
    frame_allocator: &mut impl FrameAllocator<Size4KiB>,
) -> Result<(), MapToError<Size4KiB>> {
    let page_range = {
        let heap_start = VirtAddr::new(HEAP_START as u64);
        let heap_end = heap_start + HEAP_SIZE - 1u64;
        let heap_start_page = Page::containing_address(heap_start);
        let heap_end_page = Page::containing_address(heap_end);
        Page::range_inclusive(heap_start_page, heap_end_page)
    };

    for page in page_range {
        let frame = frame_allocator
            .allocate_frame()
            .ok_or(MapToError::FrameAllocationFailed)?;
        let flags = PageTableFlags::PRESENT | PageTableFlags::WRITABLE;
        unsafe { mapper.map_to(page, frame, flags, frame_allocator)?.flush() };
    }

    unsafe {
        ALLOCATOR.lock().init(HEAP_START, HEAP_SIZE);
    }

    Ok(())
}
```

### 步骤 2：更新 Cargo.toml

```toml
[dependencies]
linked_list_allocator = "0.10"
```

### 步骤 3：使用堆分配

```rust
use alloc::{vec, vec::Vec, boxed::Box};

fn main() {
    // 现在可以使用 Vec 了！
    let mut vec = Vec::new();
    vec.push(1);
    vec.push(2);
    vec.push(3);

    // 也可以使用 Box
    let x = Box::new(41);
    println!("heap value at {:p}", x);
}
```

---

## 🔍 关键概念

### 1. 全局分配器

Rust 使用 `#[global_allocator]` 属性来指定全局分配器。所有分配请求都会通过这个分配器。

### 2. 堆内存区域

堆是一个连续的内存区域，需要：
- 分配物理页
- 映射到虚拟地址空间
- 初始化分配器

### 3. alloc crate

`alloc` crate 提供了集合类型，但需要全局分配器。

---

## 🎓 下一步

现在您已经实现了堆分配。在下一个教程中，我们将：
- 实现自定义分配器
- 学习不同的分配器设计
- 优化内存分配性能

---

## 📚 参考资源

- **原文**：https://os.phil-opp.com/heap-allocation/
- **参考代码**：`../ref_code/blog_os` 的 `post-10` 分支
- **alloc crate**：https://doc.rust-lang.org/alloc/

---

**教程完成！** ✅ 您现在可以继续学习下一个教程：[11_分配器设计.md](11_分配器设计.md)
